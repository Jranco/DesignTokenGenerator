// Generated file — do not edit manually.
import SwiftUI
import UIKit

extension Gradient {
{% for gradient in gradients %}
    static var {{ gradient.name }}: Gradient {
        Gradient(stops: [
{% for stop in gradient.stops %}
            .init(color: Color(UIColor(named: "{{ stop.assetName }}", in: .module, compatibleWith: nil)!), location: {{ stop.position }}),
{% endfor %}
        ])
    }
{% endfor %}
}
