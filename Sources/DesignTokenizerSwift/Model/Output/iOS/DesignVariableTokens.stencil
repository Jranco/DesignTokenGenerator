// Generated file — DO NOT EDIT
// swiftlint:disable all

import Foundation

enum Tokens {
{% for token in tokens %}
    {% if token.isBoolean and token.boolValue is not none %}
    static let {{ token.name }}: Bool = {{ token.boolValue }}

    {% elif token.isNumber and token.numberValue is not none %}
    static let {{ token.name }}: Double = {{ token.numberValue }}

    {% elif token.isString and token.stringValue is not none %}
    static let {{ token.name }}: String = "{{ token.stringValue }}"
    {% endif %}
{% endfor %}
}
